# HelloWorld (C# console)

This is a minimal C# console app that prints `Hello, World!`.

Prerequisite: .NET SDK (6.0/7.0) installed: https://dotnet.microsoft.com/download

Run from PowerShell:

```powershell
cd 'c:\Users\vikalp\Desktop\GithubCourse'
dotnet run
```
